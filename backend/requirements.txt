# FastAPI 框架
fastapi==0.110.0
uvicorn[standard]==0.27.1

# 数据库
sqlalchemy==2.0.25
asyncpg==0.29.0               # PostgreSQL async 驱动
psycopg2-binary==2.9.9        # PostgreSQL sync 驱动（回测线程用）

# 数据验证
pydantic==2.5.3
pydantic-settings==2.1.0

# 数据处理
pandas==2.1.4
numpy==1.26.3

# 认证
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP 客户端
httpx>=0.25.0,<0.26.0   # LLM API 调用（与 mootdx 兼容）
requests>=2.31.0       # 数据采集（同花顺/东财/北向）
mootdx>=0.10            # 通达信行情+财务（TCP 7709，不封IP）

# 重试（DeepSeek API 调用）
tenacity>=8.0

# 拼音
pypinyin>=0.51.0

# 工具
python-multipart==0.0.6
python-dotenv==1.0.0

# 开发依赖
black==23.12.1
ruff==0.1.9
pytest==7.4.4
pytest-asyncio==0.23.3
