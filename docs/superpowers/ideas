# Done

- 增加资金流
- 利用更多的热点等
- 新增策略 - 底背离+神奇九转
- 给个股跑分的时候，也要加到回测页面，这样可以看具体某一天
- 回测的时候目前只能某一天，增加功能跑一个周期
- 检查一下 checkup 策略的数据源，不是我的源数据库吗
- 回测推荐清单，点击股票后画出过去 1 年的 k 线图
- 新功能，给系统一个股票和一个时间段的 k 线图，让它总结合理的量化指标做成策略。这样寻找下一个牛股，甚至增加当时消息面的信息
- bug - 生成策略失败，华天 - 4.8，但是后面重新进入策略发现又生成了
- 运行回测，系统卡死 -- 回测已改为线程池执行
- AI Builder 页面卸载时停掉轮询 (clearTimeout)，避免内存泄漏
- 重新进入 AI Builder 时检查进行中的任务，自动恢复轮询
- 抽公共 StockSearchLookup 组件 -- BacktestForm/StrategyDetail/AIStrategyBuilder/BacktestList 四个页面复制了相同的股票搜索样板代码，应抽成 shared 组件
- 删掉策略的逻辑是什么，如何能重新恢复过来 - 状态重置的问题。回测报告同理
- 点击删除已经删除的策略是什么逻辑？filter 被自动设置成了变成活跃。回测报告同理
- 增加策略的教育功能，例如提供一些策略的教程、案例分析等，帮助用户更好地理解和应用策略
- 仪表盘的快速入门简单说一下批量回测的功能
- dashboard 页面快捷，上传策略脚本改成 AI参考选股
- Admin 用户管理页面，增加用户上次登录时间
- 20 自然语言策略构建器 — 在可视化构建器中增加「相似度匹配」模式，自然语言 → DeepSeek 识别因子 → 确认后生成相似度策略。AI 生成的因子沉淀到因子库
- 黑夜模式所有表格 header 都不可见了
- 新用户首次登录 onboarding walkthrough 引导（6 步引导遮罩，antd Tour 实现）
- 37 上传策略代码没用了， 可以删除
- 21 基于基本面的策略 — Phase 1 财报数据抓取 + 数据库建表 ✅ | Phase 2 基本面因子库 + 回测引擎集成 ✅（8 因子：ROE/PE/PB/净利润增长/营收增长/毛利率/净利率/负债率/现金流比）
- 市场热度页面 — 全市场温度（5维度）+ 四大指数板块温度（3维度）+ 板块资金流热力图 + 主题词云 + 热门股票/龙虎榜 + 北向资金趋势 + 涨跌分布图（支持板块筛选）+ 领涨/领跌板块个股弹窗

# In progress

（无）

# Ideas

- 22 增加策略的可视化展示，例如策略的收益曲线、风险指标等，帮助用户更好地理解策略的表现。
- 23 增加策略的优化功能，例如通过遗传算法、粒子群优化等方法寻找最优的策略参数。
- 24 增加策略的组合功能，例如将多个策略组合成一个更强大的策略，或者根据不同的市场环境切换不同的策略。
- 25 增加策略的风险管理功能，例如设置止损、止盈、仓位控制等，帮助用户更好地控制风险。
- 27 增加策略的实盘功能，例如将策略应用到实际的交易中，帮助用户更好地实现盈利。
- 28 增加策略的社区功能，例如用户可以分享自己的策略，或者查看其他用户的策略，帮助用户更好地学习和交流。
- 30 Admin 用户策略过滤 - 目前 Admin 用户可以看到所有用户的策略，增加过滤功能只显示自己的策略。
- 32 增加帮助页面，介绍策略的概念、如何使用策略、常见问题解答等，帮助用户更好地理解和使用策略。和介绍页面一样，但是更侧重于功能的使用。
- 35 暂时改成所有用户都能看到所有策略，但是只有自己创建的策略可以删除和改动，但是可以回测其他用户的策略。
- 36 加进结合当前市场情绪，增加选择策略的策略。
- 把批量回测报告整合到回测报告中
- Phase 3 基本面策略前端优化 — 可视化构建器中基本面因子的拖拽配置、AI 参考选股（财务指标相似度匹配）、多因子混合策略交互优化


对于策略深度的深度研究 - 对于可用性
对报告用 AI 深度研究 - 
其实还可以让 AI 分析一下这个策略符合哪些时期， 哪些时期都发生了什么事
分板块观察， 分时期观察。等等， 也可以再跟 AI 好好聊聊

对于趋势策略， 需要做调整， 比如止盈， 止损的方案。 报告也需要调整。
批量回测报告需要 HTML

Must Have
运行回测 版本 2 - 增加固定因子后直接算收益率,比如：
1. 收盘价<止跌日最低价止损
2. 收盘<ma10*0.93,缓冲两天卖出
3. 止盈 5%， 可否超过 7%回撤到 5% 止盈， 诸如此类


理解一下 sentity 四步追问法

